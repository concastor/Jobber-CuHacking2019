$(document).ready(function(res) {
  //let jobs = JSON.parse(getjobs);
  for ( let i=0; i < 5; i++){
    // var v = document.createElement('div');
    // v.class="d-flex align-items-center p-2 my-2 text-black-50 bg-white rounded box-shadow";
    // let x = document.createElement("div");
    // x.class = "col-sm-2 y-3 pl-2 col-md-2 sidebar";
    // v.appendChild(x);
    // let p = document.createElement("p");
    // p.class = "media-body  mb-0 small border-gray";
    // p.TEXT_NODE = "bitch" //jobs[i].cateogry;
    // x.appendChild(p);
    // //spaceing
    // x = document.createElement("div");
    // x.class = 'col-md-8 px-1' 
    // v.appendChild(x);
    // p = document.createElement("p");
    // p.class = "media-body pl-3 pb-3 mb-0 small lh-125 border-right border-left border-gray";
    // t = document.createElement("strong");
    // t.class ='h6 d-block text-gray-dark';
    // t.TEXT_NODE = "fuck"//jobs[i].description;
    // p.appendChild(t);
    // x.appendChild(p);

    let input = document.createElement("input");
    input.type = "Button";
    input.class = "btn btn-light btn-large"; // set the CSS class
    container.appendChild(input);
  }

  
  })
  